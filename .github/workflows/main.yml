name: Verify KSP2 Mod

on:
  push:

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Verify KSP2 Mod
        uses: Rexicon226/VerifierAction@V0.3
